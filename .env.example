# 复制为 .env.local 后执行 npm run dev / npm run build
# 列表数据优先级：NEXT_PUBLIC_PROMPTS_WORKER_URL > public/prompts-worker.json > /prompts.json
# Cloudflare Pages 只跑 npm run build、未配环境变量时，会靠 prompts-worker.json 走 Worker

NEXT_PUBLIC_PROMPTS_WORKER_URL=https://nano-prompts-api.farr1102work.workers.dev
